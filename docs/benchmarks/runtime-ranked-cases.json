{
  "setup": [
    "SET plan_cache_mode=force_generic_plan",
    "SET jit=off",
    "PREPARE or_literal(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND true ORDER BY score DESC LIMIT 10 OFFSET 0",
    "PREPARE or_runtime(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND true ORDER BY score DESC LIMIT $2 OFFSET $3",
    "PREPARE or_expression(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND true ORDER BY score DESC LIMIT ($2+0) OFFSET $3",
    "PREPARE or100_literal(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND true ORDER BY score DESC LIMIT 100 OFFSET 20",
    "PREPARE or100_runtime(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND true ORDER BY score DESC LIMIT $2 OFFSET $3",
    "PREPARE or100_expression(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND true ORDER BY score DESC LIMIT ($2+0) OFFSET $3",
    "PREPARE filtered_literal(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND id <= 25000 ORDER BY score DESC LIMIT 10 OFFSET 0",
    "PREPARE filtered_runtime(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND id <= 25000 ORDER BY score DESC LIMIT $2 OFFSET $3",
    "PREPARE filtered_expression(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND id <= 25000 ORDER BY score DESC LIMIT ($2+0) OFFSET $3",
    "PREPARE phrase_literal(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND true ORDER BY score DESC LIMIT 10 OFFSET 0",
    "PREPARE phrase_runtime(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND true ORDER BY score DESC LIMIT $2 OFFSET $3",
    "PREPARE phrase_expression(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND true ORDER BY score DESC LIMIT ($2+0) OFFSET $3",
    "PREPARE rare_literal(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND true ORDER BY score DESC LIMIT 10 OFFSET 0",
    "PREPARE rare_runtime(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND true ORDER BY score DESC LIMIT $2 OFFSET $3",
    "PREPARE rare_expression(text,bigint,bigint) AS SELECT id,stannum.full_score(ctid) AS score FROM documents WHERE body ==> $1 AND true ORDER BY score DESC LIMIT ($2+0) OFFSET $3"
  ],
  "queries": [
    [
      "or_literal",
      "EXECUTE or_literal('history OR war',10,0)"
    ],
    [
      "or_runtime",
      "EXECUTE or_runtime('history OR war',10,0)"
    ],
    [
      "or_expression",
      "EXECUTE or_expression('history OR war',10,0)"
    ],
    [
      "or100_literal",
      "EXECUTE or100_literal('history OR war',100,20)"
    ],
    [
      "or100_runtime",
      "EXECUTE or100_runtime('history OR war',100,20)"
    ],
    [
      "or100_expression",
      "EXECUTE or100_expression('history OR war',100,20)"
    ],
    [
      "filtered_literal",
      "EXECUTE filtered_literal('history OR war',10,0)"
    ],
    [
      "filtered_runtime",
      "EXECUTE filtered_runtime('history OR war',10,0)"
    ],
    [
      "filtered_expression",
      "EXECUTE filtered_expression('history OR war',10,0)"
    ],
    [
      "phrase_literal",
      "EXECUTE phrase_literal('\"united states\"',10,0)"
    ],
    [
      "phrase_runtime",
      "EXECUTE phrase_runtime('\"united states\"',10,0)"
    ],
    [
      "phrase_expression",
      "EXECUTE phrase_expression('\"united states\"',10,0)"
    ],
    [
      "rare_literal",
      "EXECUTE rare_literal('quasar',10,0)"
    ],
    [
      "rare_runtime",
      "EXECUTE rare_runtime('quasar',10,0)"
    ],
    [
      "rare_expression",
      "EXECUTE rare_expression('quasar',10,0)"
    ]
  ]
}
